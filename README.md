# sandbox-nginx-mirror

Sandbox for nginx `ngx_http_mirror_module` module

## Usage

```sh
$ docker-compose up

$ curl http://localhost
Hello, backend!

$ curl http://localhost/ping
Pong, backend!
```
